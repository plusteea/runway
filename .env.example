# Classic GitHub PAT with the gist scope. Used at build time for GitHub Pages.
VITE_GITHUB_TOKEN=
# Optional. Leave empty on first run — the app will create a secret gist and you can copy the id.
VITE_GIST_ID=
